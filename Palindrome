
## Palindrome
def palindrome(word):
    for i in range(1, len(word)+1):
        #rint(i)
        if word[i-1] == word[-i:]:
            print(i)
        else:
            print("No.")

word = str(input("Enter a word:\n"))
print(word[-1:])
palindrome(word)
